while True:
    line = input().split(" ")
    a, b = line
    a = int(a)
    b = int(b)

    list = [a, b]

    x = min(list)
    y = max(list)
    sum = 0
    if x<=0 or y<=0:
        break

    for i in range(x, y + 1):
        sum = sum + i
        print(i, end=" ")

    print("Sum=%d" % sum)
